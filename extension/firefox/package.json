{
  "name": "mister-groovy-relay-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Companion browser extension for the MiSTer GroovyRelay bridge.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "web-ext lint --source-dir=. --ignore-files test/** --ignore-files vitest.config.js --ignore-files package*.json --ignore-files node_modules/** --ignore-files dist/** --ignore-files build.sh --ignore-files go.mod --ignore-files *.md --ignore-files .gitignore",
    "build": "bash build.sh"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "vitest": "^4.1.0",
    "jsdom": "^29.0.0",
    "msw": "^2.13.0",
    "web-ext": "^10.0.0"
  }
}
