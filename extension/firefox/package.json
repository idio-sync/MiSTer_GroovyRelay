{
  "name": "mister-groovy-relay-companion",
  "version": "0.3.2",
  "private": true,
  "description": "Companion browser extension for the MiSTer GroovyRelay bridge.",
  "type": "module",
  "scripts": {
    "check:versions": "node scripts/check-versions.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "web-ext lint --source-dir=. --ignore-files test/** --ignore-files scripts/** --ignore-files vitest.config.js --ignore-files package*.json --ignore-files node_modules/** --ignore-files dist/** --ignore-files build.sh --ignore-files go.mod --ignore-files *.md --ignore-files .gitignore --ignore-files .amo-upload-uuid",
    "build": "bash build.sh",
    "sign:amo": "web-ext sign --source-dir=. --artifacts-dir=dist/amo-signed --channel=unlisted --approval-timeout=900000 --ignore-files \"test/**\" \"scripts/**\" vitest.config.js \"package*.json\" \"node_modules/**\" \"dist/**\" build.sh go.mod \"*.md\" .gitignore .amo-upload-uuid"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "vitest": "^4.1.0",
    "jsdom": "^29.0.0",
    "msw": "^2.13.0",
    "web-ext": "^10.0.0"
  }
}
